{
    "name": "circuits",
    "version": "1.0.0",
    "description": "Plight Phase 3 ZK Circuits",
    "scripts": {
        "test": "mocha test",
        "build": "circom src/main.circom --r1cs --wasm --sym --output build",
        "compile:commitment": "circom src/commitment.circom --r1cs --wasm --sym --output build",
        "compile:main": "circom src/main.circom --r1cs --wasm --sym --output build"
    },
    "dependencies": {
        "circomlib": "^2.0.5",
        "circomlibjs": "^0.1.7",
        "snarkjs": "^0.7.5"
    },
    "devDependencies": {
        "chai": "^4.3.7",
        "circom_tester": "^0.0.20",
        "ffjavascript": "^0.2.63",
        "mocha": "^10.2.0"
    }
}
