# Aggregation Engine

The Aggregation Engine is the core logic library for the Plight protocol. It contains the adapters and aggregation logic to process user activity across different chains and protocols.
